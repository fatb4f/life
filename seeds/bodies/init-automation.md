## Goal
Set up Makefile-driven ops to manage labels, milestones, views, and seed issues.

### Done when
- `make plan` passes
- `make init` creates resources without duplicates
- README documents workflow